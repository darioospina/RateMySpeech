const Event = require('../models/eventsModel')
const mongoose = require('mongoose')

// get all event

// get a single event

// create new event

// update a new event

// delete a event

module.exports = {
    getEvents,
    getEvent,
    createEvent,
    deleteEvent,
    updateEvent
}
